{
  "name": "timekeeper",
  "version": "4.0.17",
  "private": true,
  "description": "Timekeeper userscript TypeScript project",
  "scripts": {
    "prebuild": "node -p \"'export const TIMEKEEPER_VERSION = ' + JSON.stringify(require('./package.json').version) + ';'\" > src/version.ts",
    "build": "npm run prebuild && node scripts/build.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.4.0"
  }
}
